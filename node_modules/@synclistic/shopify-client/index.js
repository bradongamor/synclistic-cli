const { fetchRequest, graphqlRequest } = require('./src/request');
const { createStoreClient } = require('./src/client');
const {
  buildAdminGraphqlUrl,
  normalizeShopDomain,
  normalizeStoreIdentity
} = require('./src/store-url');
const {
  ShopifyClientError,
  ShopifyHttpError,
  ShopifyGraphQLError,
  normalizeClientError
} = require('./src/errors');
const {
  createPagesClient,
  PAGES_QUERY,
  PAGE_CREATE_MUTATION,
  PAGE_UPDATE_MUTATION,
  PAGE_DELETE_MUTATION
} = require('./src/objects/pages');
const {
  createBlogsClient,
  BLOGS_QUERY,
  BLOG_CREATE_MUTATION,
  BLOG_UPDATE_MUTATION,
  BLOG_DELETE_MUTATION
} = require('./src/objects/blogs');
const {
  createArticlesClient,
  ARTICLES_QUERY,
  BLOG_LOOKUP_QUERY,
  ARTICLE_CREATE_MUTATION,
  ARTICLE_UPDATE_MUTATION,
  ARTICLE_DELETE_MUTATION
} = require('./src/objects/articles');
const {
  createCollectionsClient,
  createCollectionInput,
  COLLECTIONS_QUERY,
  COLLECTION_CREATE_MUTATION,
  COLLECTION_UPDATE_MUTATION,
  COLLECTION_DELETE_MUTATION
} = require('./src/objects/collections');
const { createProductsClient } = require('./src/objects/products');
const { createMediaClient } = require('./src/objects/media');
const { createMenusClient } = require('./src/objects/menus');
const {
  createThemesClient,
  THEMES_QUERY,
  THEME_FILES_QUERY,
  THEME_FILES_UPSERT_MUTATION,
  THEME_FILES_DELETE_MUTATION
} = require('./src/objects/themes');

module.exports = {
  fetchRequest,
  graphqlRequest,
  createStoreClient,
  buildAdminGraphqlUrl,
  normalizeShopDomain,
  normalizeStoreIdentity,
  ShopifyClientError,
  ShopifyHttpError,
  ShopifyGraphQLError,
  normalizeClientError,
  createPagesClient,
  createBlogsClient,
  createArticlesClient,
  createCollectionsClient,
  createProductsClient,
  createMediaClient,
  createMenusClient,
  createThemesClient,
  createCollectionInput,
  PAGES_QUERY,
  PAGE_CREATE_MUTATION,
  PAGE_UPDATE_MUTATION,
  PAGE_DELETE_MUTATION,
  BLOGS_QUERY,
  BLOG_CREATE_MUTATION,
  BLOG_UPDATE_MUTATION,
  BLOG_DELETE_MUTATION,
  ARTICLES_QUERY,
  BLOG_LOOKUP_QUERY,
  ARTICLE_CREATE_MUTATION,
  ARTICLE_UPDATE_MUTATION,
  ARTICLE_DELETE_MUTATION,
  COLLECTIONS_QUERY,
  COLLECTION_CREATE_MUTATION,
  COLLECTION_UPDATE_MUTATION,
  COLLECTION_DELETE_MUTATION,
  THEMES_QUERY,
  THEME_FILES_QUERY,
  THEME_FILES_UPSERT_MUTATION,
  THEME_FILES_DELETE_MUTATION
};
